import { FC } from "react";

const Title: FC<{ title: string }> = ({ title }) => {
  // ! DO TITLE STYLES
  // ! DO TITLE STYLES
  // ! DO TITLE STYLES
  // ! DO TITLE STYLES
  return (
    <>
      <div>
        <h3>{title}</h3>
      </div>
    </>
  );
};

export default Title;
